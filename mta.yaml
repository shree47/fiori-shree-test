_schema-version: "2.0.0"
ID: "fiori-shree-test"
version: 1.1.0

parameters:
  hcp-deployer-version: "1.0.0"

modules:
  - name: "fiori-shree-test"
    type: html5
    path: .
    parameters:
       version: ${timestamp}
    build-parameters:
      builder: grunt
      build-result: dist